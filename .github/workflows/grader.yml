name: Grader Verification Workflow
on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  verify-student:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: 24f2000058@ds.study.iitm.ac.in
        run: echo "Verifying CI/CD setup for student 24f2000058"
